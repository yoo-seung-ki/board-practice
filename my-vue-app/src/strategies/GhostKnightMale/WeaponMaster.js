// WeaponMaster.js
export default class WeaponMaster {
    constructor(characterInfo) {
        this.characterInfo = characterInfo;
    }

    calculateDamage(skillId, levelInfo) {
        // 웨펀마스터의 스킬 데미지 계산 로직 구현
    }
}